questions:
  - question: Where are Azure OpenAI environment variables configured?
    expected_paths:
      - env-vars.md
  - question: How does the chat UI stream responses?
    expected_paths:
      - plugins/ui
  - question: Where is the search embedding flow defined?
    expected_paths:
      - services
      - plugins
  - question: How do local services mount plugins?
    expected_paths:
      - docker-compose-local.yml
      - docker-compose.yml
  - question: Where are D2E CLI commands defined?
    expected_paths:
      - scripts
      - services

