# OpenCode Adapter

OpenCode-specific wrappers live here. Shared workflow policy lives in `skills/`; wrappers in `.opencode/commands` and `.opencode/skills` should only point back to the canonical shared skill files.
