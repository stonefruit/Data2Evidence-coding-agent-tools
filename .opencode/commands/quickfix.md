---
description: Run the shared quickfix workflow
---

Load and follow the shared workflow skill at `skills/quickfix/SKILL.md`.

Arguments: `$ARGUMENTS`

This command is a thin OpenCode compatibility wrapper. Do not duplicate workflow policy here.
