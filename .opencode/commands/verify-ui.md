---
description: Run the shared verify-ui workflow
---

Load and follow the shared workflow skill at `skills/verify-ui/SKILL.md`.

Arguments: `$ARGUMENTS`

This command is a thin OpenCode compatibility wrapper. Do not duplicate workflow policy here.
