---
description: Run the shared cohorts-dev workflow
---

Load and follow the shared workflow skill at `skills/cohorts-dev/SKILL.md`.

Arguments: `$ARGUMENTS`

This command is a thin OpenCode compatibility wrapper. Do not duplicate workflow policy here.
