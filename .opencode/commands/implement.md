---
description: Run the shared implement workflow
---

Load and follow the shared workflow skill at `skills/implement/SKILL.md`.

Arguments: `$ARGUMENTS`

This command is a thin OpenCode compatibility wrapper. Do not duplicate workflow policy here.
