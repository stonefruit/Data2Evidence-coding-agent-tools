---
description: Run the shared dev-ui-cohorts workflow
---

Load and follow the shared workflow skill at `skills/dev-ui-cohorts/SKILL.md`.

Arguments: `$ARGUMENTS`

This command is a thin OpenCode compatibility wrapper. Do not duplicate workflow policy here.
