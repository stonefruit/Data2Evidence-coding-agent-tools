---
description: Run the shared smallfeature workflow
---

Load and follow the shared workflow skill at `skills/smallfeature/SKILL.md`.

Arguments: `$ARGUMENTS`

This command is a thin OpenCode compatibility wrapper. Do not duplicate workflow policy here.
