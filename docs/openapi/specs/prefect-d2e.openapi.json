{
  "openapi": "3.1.0",
  "info": {
    "title": "Data2Evidence Prefect D2e API",
    "version": "0.1.0",
    "description": "Source-derived OpenAPI spec for one Data2Evidence service/function. Tags describe UI usage, backend coverage, and handler trace state."
  },
  "servers": [
    {
      "url": "/"
    }
  ],
  "tags": [
    {
      "name": "handler-unresolved",
      "description": "The route is known from UI or route metadata, but no concrete backend handler was found."
    },
    {
      "name": "service:prefect-d2e",
      "description": "Operation belongs to the prefect-d2e service/function surface."
    },
    {
      "name": "ui-used",
      "description": "Operation is called by at least one Data2Evidence UI."
    },
    {
      "name": "ui:jobs",
      "description": "Operation is called by the jobs UI."
    }
  ],
  "paths": {
    "/prefect/d2e/ui-settings": {
      "get": {
        "operationId": "getPrefectD2eUiSettings",
        "summary": "if",
        "tags": [
          "handler-unresolved",
          "service:prefect-d2e",
          "ui-used",
          "ui:jobs"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UnknownJson"
                },
                "example": {
                  "api_url": "/prefect/d2e/api",
                  "csrf_enabled": false,
                  "flags": {}
                }
              }
            }
          },
          "default": {
            "description": "Error response"
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    },
    "schemas": {
      "UnknownJson": {
        "description": "Shape not inferred from UI or backend source.",
        "type": "object",
        "additionalProperties": true
      }
    }
  },
  "security": [
    {
      "bearerAuth": []
    }
  ]
}
