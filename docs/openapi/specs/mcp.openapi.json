{
  "openapi": "3.1.0",
  "info": {
    "title": "Data2Evidence Mcp API",
    "version": "0.1.0",
    "description": "Source-derived OpenAPI spec for one Data2Evidence service/function. Tags describe UI usage, backend coverage, and handler trace state."
  },
  "servers": [
    {
      "url": "/"
    }
  ],
  "tags": [
    {
      "name": "backend-implemented",
      "description": "A backend route declaration was found for this operation."
    },
    {
      "name": "backend-only",
      "description": "A backend route exists, but no current UI call was found."
    },
    {
      "name": "backend:mcp-server",
      "description": "Operation is declared by the mcp-server backend function."
    },
    {
      "name": "handler-found",
      "description": "The backend route was traced to a concrete controller/router handler."
    },
    {
      "name": "service:mcp",
      "description": "Operation belongs to the mcp service/function surface."
    }
  ],
  "paths": {
    "/mcp/chat": {
      "post": {
        "operationId": "postMcpChat",
        "summary": "register Routes",
        "tags": [
          "backend-implemented",
          "backend-only",
          "backend:mcp-server",
          "handler-found",
          "service:mcp"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UnknownJson"
                },
                "example": {
                  "jsonrpc": "2.0",
                  "id": 1,
                  "result": {
                    "tools": []
                  }
                }
              }
            }
          },
          "default": {
            "description": "Error response"
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UnknownJson"
              },
              "example": {
                "jsonrpc": "2.0",
                "id": 1,
                "method": "tools/list",
                "params": {}
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    },
    "schemas": {
      "UnknownJson": {
        "description": "Shape not inferred from UI or backend source.",
        "type": "object",
        "additionalProperties": true
      }
    }
  },
  "security": [
    {
      "bearerAuth": []
    }
  ]
}
