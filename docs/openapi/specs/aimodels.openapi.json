{
  "openapi": "3.1.0",
  "info": {
    "title": "Data2Evidence Aimodels API",
    "version": "0.1.0",
    "description": "Source-derived OpenAPI spec for one Data2Evidence service/function. Tags describe UI usage, backend coverage, and handler trace state."
  },
  "servers": [
    {
      "url": "/"
    }
  ],
  "tags": [
    {
      "name": "handler-unresolved",
      "description": "The route is known from UI or route metadata, but no concrete backend handler was found."
    },
    {
      "name": "service:aimodels",
      "description": "Operation belongs to the aimodels service/function surface."
    },
    {
      "name": "ui-used",
      "description": "Operation is called by at least one Data2Evidence UI."
    },
    {
      "name": "ui:portal",
      "description": "Operation is called by the portal UI."
    }
  ],
  "paths": {
    "/aimodels/build/assets.json": {
      "get": {
        "operationId": "getAimodelsBuildAssetsJson",
        "summary": "add Prefix",
        "tags": [
          "handler-unresolved",
          "service:aimodels",
          "ui-used",
          "ui:portal"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UnknownJson"
                },
                "example": {
                  "js": [
                    "/assets/index.js"
                  ],
                  "css": [
                    "/assets/index.css"
                  ]
                }
              }
            }
          },
          "default": {
            "description": "Error response"
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    },
    "schemas": {
      "UnknownJson": {
        "description": "Shape not inferred from UI or backend source.",
        "type": "object",
        "additionalProperties": true
      }
    }
  },
  "security": [
    {
      "bearerAuth": []
    }
  ]
}
