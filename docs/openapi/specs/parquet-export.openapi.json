{
  "openapi": "3.1.0",
  "info": {
    "title": "Data2Evidence Parquet Export API",
    "version": "0.1.0",
    "description": "Source-derived OpenAPI spec for one Data2Evidence service/function. Tags describe UI usage, backend coverage, and handler trace state."
  },
  "servers": [
    {
      "url": "/"
    }
  ],
  "tags": [
    {
      "name": "backend-implemented",
      "description": "A backend route declaration was found for this operation."
    },
    {
      "name": "backend-only",
      "description": "A backend route exists, but no current UI call was found."
    },
    {
      "name": "backend:parquet-export",
      "description": "Operation is declared by the parquet-export backend function."
    },
    {
      "name": "handler-found",
      "description": "The backend route was traced to a concrete controller/router handler."
    },
    {
      "name": "service:parquet-export",
      "description": "Operation belongs to the parquet-export service/function surface."
    }
  ],
  "paths": {
    "/parquet-export": {
      "post": {
        "operationId": "postParquetExport",
        "summary": "catch",
        "tags": [
          "backend-implemented",
          "backend-only",
          "backend:parquet-export",
          "handler-found",
          "service:parquet-export"
        ],
        "parameters": [],
        "responses": {
          "200": {
            "description": "Successful response",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UnknownJson"
                }
              }
            }
          },
          "201": {
            "description": "Created",
            "content": {
              "application/json": {
                "schema": {
                  "$ref": "#/components/schemas/UnknownJson"
                },
                "example": [
                  {
                    "person_id": 1,
                    "value": "example"
                  }
                ]
              }
            }
          },
          "default": {
            "description": "Error response"
          }
        },
        "requestBody": {
          "required": true,
          "content": {
            "application/json": {
              "schema": {
                "$ref": "#/components/schemas/UnknownJson"
              },
              "example": {
                "datasetId": "4f05abcf-36d6-4e88-a44d-ad1ee3a0b06e",
                "cohortId": 200001,
                "templateId": "example-template",
                "name": "example-query",
                "format": "json",
                "conceptIds": [
                  100001
                ],
                "yearRange": {
                  "from": "2020",
                  "to": "2024"
                }
              }
            }
          }
        }
      }
    }
  },
  "components": {
    "securitySchemes": {
      "bearerAuth": {
        "type": "http",
        "scheme": "bearer",
        "bearerFormat": "JWT"
      }
    },
    "schemas": {
      "UnknownJson": {
        "description": "Shape not inferred from UI or backend source.",
        "type": "object",
        "additionalProperties": true
      }
    }
  },
  "security": [
    {
      "bearerAuth": []
    }
  ]
}
