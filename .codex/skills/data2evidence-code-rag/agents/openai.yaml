interface:
  display_name: "Data2Evidence Code RAG (Deprecated)"
  short_description: "Legacy Qdrant RAG maintenance only"
  default_prompt: "Use $data2evidence-code-rag only when explicitly maintaining or running the deprecated local code-rag tool."
policy:
  allow_implicit_invocation: false
